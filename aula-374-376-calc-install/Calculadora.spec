# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['..\\aula-346-373-calc\\main.py'],
    pathex=[],
    binaries=[],
    datas=[('../aula-346-373-calc/files/', './files/')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='Calculadora',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['..\\aula-346-373-calc\\files\\icon.png'],
)
